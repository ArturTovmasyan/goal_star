
{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') and object.position < admin.maxPosition %}
    <a href="{{ admin.generateObjectUrl('down', object) }}" class="btn btn-sm btn-default view_link" title="down">
        <i class="glyphicon glyphicon-arrow-down"></i></a>
{% endif %}
