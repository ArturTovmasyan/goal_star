
{% if admin.isGranted('VIEW', object) and admin.hasRoute('show') and object.position > admin.minPosition %}
    <a href="{{ admin.generateObjectUrl('up', object) }}" class="btn btn-sm btn-default view_link" title="up">
        <i class="glyphicon glyphicon-arrow-up"></i></a>
{% endif %}